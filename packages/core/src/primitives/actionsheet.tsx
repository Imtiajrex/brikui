import NativeActionSheet, { ActionSheetProps, ActionSheetRef } from 'react-native-actions-sheet';
import { withUniwind } from 'uniwind';
const ActionSheet = withUniwind(NativeActionSheet);
export type { ActionSheetProps, ActionSheetRef };

export default ActionSheet;
